# Wireless 开发状态

## 当前阶段

- 当前阶段：W0 · 协议对齐（进行中）
- 真实开工日期：2026-08-31
- ⑤ 拥有权验证：未完成
- 已完成阶段：无

## 每阶段关键决策及理由

### W0

- 采用《2026 年 9 月 双项目升级执行协议 v6》作为唯一上位基线，旧计划只作归档背景。
- 正式 `run_seed` 预注册为 `20260901` 至 `20260905`；五个 seed 只做描述性统计，避免结果产生后挑 seed。
- 固定沿用 V1 的 train/validation 开发边界；V1 test 仅允许索引/哈希互斥检查，禁止读取信号、标签或重新评测。
- 实验矩阵冻结为 A0、A1/A2-T、A2-G、A3 共 4 个配置 × 5 seed；A2-T 复用 A1，不重复训练。
- W3 只保留聚合方式和 Dropout 两条独立一维比较，不扩展 temporal bins 或外部数据。

## 未决问题

- W0 的单次现有训练流程耗时尚待测量；若显著超过约 71 秒，必须在任何正式结果产生前由用户决定是否调整 seed 数量。
- 两份未跟踪旧 V2 文档尚待按 v6 分诊归档。
- W0 的三道拥有权问题尚未完成。

## 下一阶段入口条件

只有同时满足以下条件，才能由用户明确下令开始 W1：

1. `docs/v2_protocol.md` 已在任何正式结果前提交；
2. 旧 V2 文档已分诊并单独提交；
3. 单次训练耗时已确认；
4. W0 学习讲解和三道具体问题已完成；
5. 本文件的“⑤ 拥有权验证”已改为“已完成”；
6. 用户明确发出 W1 开工指令。

## 环境与依赖变更

- W0 未安装、升级或删除任何依赖。
- W0 不修改业务代码和训练实现。

## 给接手者的上下文

- 本阶段尝试过但放弃的方案，以及放弃原因：旧协议的 `17, 43, 97` seed、12次矩阵、temporal bins 和 GNU Radio 计划与 v6 不一致，不进入九月 W1–W4。
- 当前实现里最容易被误改的地方：`BaselineExperimentConfig.seed` 当前同时流入 split、全局随机状态和 DataLoader；必须到 W1 才解耦，W0 不改代码。
- 下一阶段已经预见到的坑：现有 `RadioMLPartitions` 暴露 test `Subset`，W1 的训练入口必须只接收 train/validation manifest，同时保留索引级互斥检查能力。
- W0 的耗时测量不是正式实验，不得保存、读取或引用其中的验证指标。
